# # Function created using an expression using ‘lambda’ keyword.
# lambda arguments:expressions
# # can be used as a normal function
#functions gets assigne to a variable and then you can call that variable along with arguments.

square = lambda x:x*x
print(square(6))

sum = lambda a,b,c:a+b+c
print(sum(1,2,3))